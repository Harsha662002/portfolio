import React from "react";
import { Tilt } from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className="xs:w-[250px] w-full">
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className="w-full green-blue-gradient p-[1px] rounded-[20px] shadow-card"
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        // bg-[#004758]
        className=" bg-[#004778] rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col"
      >
        <img
          src={icon}
          alt="web-development"
          className="w-16 h-16 object-contain"
        />

        <h3 className="text-white text-[20px] font-bold text-center">
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);
// #00E5FF
const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className="mt-4 text-[#efefef] text-[17px] max-w-3xl leading-[30px]"
      >
        I'm a skilled Web developer with experience in TypeScript and
        JavaScript.My expertise extends to frameworks like React, Node.js, and
        NextJs. As a quick learner and a collaborative professional, I thrive on
        working closely with clients to craft not just solutions, but
        transformative experiences. My focus is on developing efficient,
        scalable, and exceptionally user-friendly solutions that not only meet
        but exceed real-world challenges. Let's join forces to turn your
        innovative ideas into vibrant, functional realities!
      </motion.p>

      <div className="mt-20 flex justify-between flex-wrap gap-10">
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
